// uniform float4x4 model : BUFFER0;

void main(
	float3 aPosition : POSITION,
	float4 aColor : COLOR,
	float4 out vPosition : POSITION,
	float4 out vColor : COLOR)
{
	// vPosition = mul(float4(aPosition, 1.f), model);
	vPosition = float4(aPosition, 1.0f);
	vColor = aColor;
}
